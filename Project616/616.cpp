#include <stdio.h>
#include <string.h>

const int maxn = 2e5 + 10;
int t, n;

int main() {
	scanf("%d", &t);
	while (t--) {
		char str1[maxn], str2[maxn];
		scanf("%d", &n);
		scanf("%s", str1 + 1);
		scanf("%s", str2 + 1);
		for (int i = 1; i <= n; i++) {
			if (str2[i] == '1') {
				if (str1) {

				}
			}
		}
	}

	return 0;
}