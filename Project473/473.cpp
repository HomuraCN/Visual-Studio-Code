#include <stdio.h>

const int maxn = 110;
int t, n, a[maxn];

int main() {
	scanf("%d", &t);
	while (t--) {
		scanf("%d", &n);
		for (int i = 1; i <= n; i++) {
			scanf("%d", &a[i]);
		}
		
	}

	return 0;
}