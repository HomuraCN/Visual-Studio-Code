#include <stdio.h>
#define LL long long

const int maxn = 510;
int n;
LL map[maxn][maxn];

int main() {
	scanf("%d", &n);
	for (int i = 1; i <= n; i++) {
		for (int j = 1; j <= n; j++) {
			scanf("%lld", &map[i][j]);
		}
	}
	for (int i = 1; i <= ) {

	}

	return 0;
}