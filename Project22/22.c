#include <stdio.h>

int main()
{
	int a = 3;
	printf("a / 2 = %d\n", a / 2);
	printf("(float)a / 2 = %f\n", (float)a / 2);
	printf("(float)(a / 2) = %f\n", (float)(a / 2));
	printf("a = %d\n", a);

	return 0;
}